# Example: reverse-proxy Node API (poker-api.aliasyazilim.com → 127.0.0.1:4000)
#
# Common CORS failure: handling OPTIONS in nginx with `return 204` WITHOUT proxying to Node.
# That response has no Access-Control-* headers → browser blocks the real POST.
# Fix: proxy ALL methods (including OPTIONS) to Node, or add matching add_header on that block.
#
# server {
#   listen 443 ssl;
#   server_name poker-api.aliasyazilim.com;
#
#   location / {
#     proxy_pass http://127.0.0.1:4000;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
