# One host: https://poker.aliasyazilim.com — SPA + /api → Node (127.0.0.1:4000)
#
# Build Angular: npm run build  →  dist/poker-planning/browser
# Point root to that folder OR use STATIC_ROOT on Express (see server README).
#
# server {
#   listen 443 ssl;
#   server_name poker.aliasyazilim.com;
#
#   root /var/www/poker-planning/browser;
#   index index.html;
#
#   location /api/ {
#     proxy_pass http://127.0.0.1:4000;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }
